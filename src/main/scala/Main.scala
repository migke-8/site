package migke.site

import migke.site.*
import migke.site.types.Input
import migke.site.attribute.AttributeMap
import migke.site.attribute.Attr

@main def run(): Unit = {
  println(
    HTMLBuilder({
      html()({

      })
    })
  )
}
